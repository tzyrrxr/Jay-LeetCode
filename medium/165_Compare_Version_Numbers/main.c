#include <stdio.h>

int compareVersion(char* version1, char* version2) {
 int ret;
 int ver1, ver2;
 int i;
 char *tmp = (char*) calloc(500, sizeof(char));

 i = 0;
 while (version1[i] != '\0') {
  if (version1[i] == '.') {
  }
 }

 return ret;
}

int main (void) {

 return 0;
}
