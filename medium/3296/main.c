long long minNumberOfSeconds(int mountainHeight, int* workerTimes, int workerTimesSize) {
  int max = INT_MIN;
  int heigh = mountainHeight - workerTimesSize;

  for (int i = 0; i < workerTimesSize; i++) {
    workerTimes[i]
  }
    
  return max;

}
